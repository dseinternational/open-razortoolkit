@page "/bootstrap/controls/datetimepicker"

<h1>DateTime Picker</h1>

<h2 class="h5 mt-4">This example is a local DateTime</h2>

<DateTimePicker DateType="HtmlInputDateType.DateTimeLocal" HeaderText="Label here" DescriptionText="Some more detailed information below!" @bind-Value="@_dateTimeValue"></DateTimePicker>

<p class="mt-3">The current value is: <code>@_dateTimeValue</code></p>

<h2 class="h5 mt-4">This example is a Date</h2>

<DateTimePicker DateType="HtmlInputDateType.Date" HeaderText="Label here" DescriptionText="Some more detailed information below!" @bind-Value="@_dateValue"></DateTimePicker>

<p class="mt-3">The current value is: <code>@_dateValue</code></p>

<h2 class="h5 mt-4">This example is a Time</h2>

<DateTimePicker DateType="HtmlInputDateType.Time" HeaderText="Label here" DescriptionText="Some more detailed information below!" @bind-Value="@_timeValue"></DateTimePicker>

<p class="mt-3">The current value is: <code>@_timeValue</code></p>


<h2 class="h5 mt-4">This example is a Month</h2>

<DateTimePicker DateType="HtmlInputDateType.Month" HeaderText="Label here" DescriptionText="Some more detailed information below!" @bind-Value="@_monthValue"></DateTimePicker>

<p class="mt-3">The current value is: <code>@_monthValue.Month.ToString("x2")/@_monthValue.Year</code></p>


@code {
    private DateTime _dateTimeValue = DateTime.Now;
    private DateOnly _dateValue = DateOnly.FromDateTime(DateTime.Now);
    private TimeOnly _timeValue = TimeOnly.FromDateTime(DateTime.Now);
    private DateOnly _monthValue = DateOnly.FromDateTime(DateTime.Now);
}
